/*
use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, Serialize, Deserialize)]
pub struct ApolloLinkShapesLieAlgErrorModelsModule {
    pub full_convex_hulls_standard_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    pub full_obbs_standard_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    pub full_bounding_spheres_standard_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    pub decomposition_convex_hulls_standard_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    pub decomposition_obbs_standard_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    pub decomposition_bounding_spheres_standard_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    
    pub full_convex_hulls_pseudo_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    pub full_obbs_pseudo_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    pub full_bounding_spheres_pseudo_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    pub decomposition_convex_hulls_pseudo_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    pub decomposition_obbs_pseudo_models: Vec<Vec<([f64; 3], [f64; 3])>>,
    pub decomposition_bounding_spheres_pseudo_models: Vec<Vec<([f64; 3], [f64; 3])>>
}
*/